return {
    launcher = require("bindings.key.launcher"),
    awesome  = require("bindings.key.awesome"),
    volume   = require("bindings.key.volume"),
    window   = require("bindings.key.window"),
    screen   = require("bindings.key.screen"),
    tag      = require("bindings.key.tag"),
}
