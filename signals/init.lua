return {
    notification = require("signals.notification"),
    layouts      = require("signals.layouts"),
    display      = require("signals.display"),
    rules        = require("signals.rules"),
}
